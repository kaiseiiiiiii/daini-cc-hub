# Firestore 初期データ投入手順

メンバーマスタ（＝ログイン許可リスト）とチームマスタを Firestore に登録する手順です。

**この作業は Firebase コンソールで行ってください。**
許可リストをリポジトリに置かないための構成なので、
ここに書かれたメールアドレスや氏名をコードやこのファイルに書き戻さないでください。

---

## 前提

- `firestore.rules` を適用済みであること
- Authentication で Google ログインを有効化済みであること

ルールにより、メンバーマスタは**クライアントから一切書き込めません**。
追加・変更・削除はすべて Firebase コンソール（またはサーバーの Admin SDK）から行います。

---

## ⚠️ 先に読んでください — コンソール入力で実際にハマった4点

初回投入（15名）で、以下のすべてを踏みました。作業前に目を通してください。

### ① 「コレクションを開始」と「ドキュメントを追加」は別物

| ボタン | 作られるもの |
|---|---|
| **＋ コレクションを開始** | 入れ物（フォルダに相当） |
| **＋ ドキュメントを追加** | 中身（ファイルに相当） |

2件目以降は必ず「**ドキュメントを追加**」を使います。
ここを間違えると、`teams/team_a` というドキュメントのつもりが
`team_a` という独立したコレクションになり、アプリからは一切見えません。

### ② 型は「引用符の有無」で見分ける

Firestore コンソールは**文字列だけを引用符付きで表示**します。

| 表示 | 型 | 判定 |
|---|---|---|
| `active: true` | boolean | ✅ |
| `active: "true"` | string | ❌ ログインできなくなる |
| `order: 24` | number | ✅ |
| `order: "24"` | string | ❌ 一覧から消える |

型のプルダウンに `number` はありません。整数は **`int64`** を選びます。

### ③ フィールド0個で保存すると「幽霊ドキュメント」になる

IDだけ作ってフィールドを保存しないと、**コンソールの一覧には出るが
API からは存在しない**ドキュメントができます。斜体で表示され、
開くと「このドキュメントは存在しないため、クエリやスナップショットに
表示されません」と出ます。

この状態になったら、削除して作り直してください。

### ④ コンソールの表示は当てにならないことがある

上記③の状態では、コンソールがフィールドを表示していても実体がありません。
**必ず API 側から読み直して確認してください。**確認方法は末尾の「3. 登録後の確認」に。

> **15名分をまとめて入れるなら、コンソール手入力より
> スクリプトからの一括投入を勧めます。** 型を保証でき、上記①〜③が原理的に起きません。
> 手順は末尾の「4. スクリプトから一括投入する」を参照してください。

---

## 1. チームマスタ `teams`

コレクション `teams` を作り、チームごとに1ドキュメント作ります。

**ドキュメントID** は英数字の任意のID（例: `mgmt` / `team_a` / `team_b`）。

| フィールド | 型 | 説明 |
|---|---|---|
| `label` | string | 画面に出すチーム名 |
| `order` | number | 表示順（小さいほど上） |
| `colorKey` | string | アバターの配色。`mgmt` / `a` / `b` のいずれか |

### 記入例（3件）

| ドキュメントID | label | order | colorKey |
|---|---|---|---|
| `mgmt` | 管理職 | 1 | `mgmt` |
| `team_a` | （リーダー名）チーム | 2 | `a` |
| `team_b` | （リーダー名）チーム | 3 | `b` |

> `label` には実際のチーム名（リーダーの姓を含む）を入れてください。
> リポジトリに置かないための Firestore 管理です。

---

## 2. メンバーマスタ `members`

コレクション `members` を作ります。**1名につき1ドキュメント**です（初回は15名分）。

### ドキュメントID = メールアドレス（すべて小文字）

```
yamada.taro@example.co.jp
```

**必ず小文字で登録してください。**
セキュリティルールはログイン時のメールアドレスを小文字化して突き合わせるため、
大文字が混ざっているとそのアカウントはログインできません。

### フィールド

| フィールド | 型 | 必須 | 説明 |
|---|---|---|---|
| `memberId` | string | ✅ | アプリ内部で使う短いID。投稿の作成者IDになる。**後から変更しないこと**（過去の投稿と紐づかなくなります） |
| `fullName` | string | ✅ | **スプレッドシート上の氏名と完全一致**させる。姓名の間は半角スペース |
| `displayName` | string | ✅ | 画面に出す短い名前（通常は姓） |
| `initial` | string | ✅ | アバターに出す1文字 |
| `team` | string | ✅ | `teams` のドキュメントID |
| `jobTitle` | string | ✅ | 役職（センター長／チーフ／リーダー／メンバー） |
| `role` | string | ✅ | 権限区分。`member` または `admin` のみ |
| `order` | number | ✅ | 表示順。**全員に重複しない値を入れること**（後述） |
| `active` | boolean | ✅ | `true` で有効。退職・異動時は削除せず `false` にする |
| `inShift` | boolean | 任意 | `false` にするとシフト関連から外れる（後述）。未設定なら載る |
| `avatar` | string | 任意 | アバター画像のファイル名（後述）。未設定なら頭文字のアバター |
| `chatUserId` | string | 任意 | Google Chat のユーザーID（数値）。Thanks の新着通知で本人を @メンションするのに使う。**手入力は不要**（GAS の `backfillChatUserIdsViaPeople` が入れます）。未設定でも通知は止まらず、その人だけ表示名で出ます |
| `canSeeTeamKpi` | boolean | 任意 | `true` にした人だけ「チーム」タブ（メンバー別の手配率・手配粗利額）を見られる。**未設定＝見られない**。`role` とは別の軸で、admin でもこれが無ければ見られません。付け外しはコンソールからのみ |

### アイコンは画面から設定できます

**通常はこのフィールドを触る必要はありません。** アプリのヘッダーにある
自分のアイコンを押すと設定画面が開き、画像を選ぶだけで保存されます。

- 画像はブラウザ側で**正方形に切り抜いて128pxに縮小**してから保存されます
- 保存先は Firestore の `avatars` コレクション（ドキュメントID = `memberId`）
- **本人と管理者だけ**が書き込めます。管理者は設定画面のメンバー一覧から
  他の人の分も設定できます（入社時などに代わりに入れる用）
- Firebase Storage は使っていません。新しいプロジェクトでは Storage に
  課金プランが必要で、15人分のアイコンのために増やす依存としては重いためです

優先順位は **本人が設定した画像 → 下の `avatar` フィールド → 頭文字** です。

### `avatar` について（任意・上級者向け）

`assets/` に画像を置いて全員に配る場合に使います。ファイル名だけを入れます。
パスやURLは入れません。

```
avatar : string   av-01.webp
```

- 受け付けるのは `webp` / `png` / `jpg` / `jpeg`
- `../` を含む値や `https://…` は無視され、頭文字のアバターに戻ります
  （外部から読ませると、誰がいつ画面を開いたかが配信元に伝わるため）
- 未設定の人は頭文字のまま。全員分が揃っていなくても混在で成立します

**ファイル名に氏名を入れないでください。** リポジトリは公開されているので、
`av-01.webp` のような通し番号にし、誰の画像かという対応はこのフィールド側で持ちます。

### `inShift` について

シフトの当番に入らない人を、シフト関連の表示と集計から外すためのフラグです。
**未設定なら従来どおり載る**ので、既存メンバーに影響はありません。

`false` にすると、次の4つから同時に外れます。

| 場所 | 効果 |
|---|---|
| シフトタブの表 | 行が出なくなる |
| 本日のシフトサマリー | 名前が出なくなる |
| ヘッダーの「本日◯名 シフト予定」 | 人数に数えない |
| 目標進捗タブの残り稼働時間 | **稼働時間に数えない** |

> ⚠️ **4つ目に注意してください。**残り稼働時間が減るぶん、
> 「1時間あたり必要なペース」は**上がります**。表示から消すだけの操作ではなく、
> その人を戦力として数えない、という意味になります。
>
> 表示からは消したいが稼働時間には数えたい、という場合はフラグを分ける必要が
> あるので、そのときは相談してください。

**この人はフィード・掲示板・アンケートには今までどおり参加できます。**
`inShift` はシフトと稼働時間の集計にしか効きません。
アプリ自体を使えなくするのは `active: false` のほうです。

### 1名分のドキュメント雛形（コピー用）

Firebase コンソールの「フィールドを追加」で、以下をそのまま入れてください。

以下は**架空の例**です。実際の値に置き換えてください。

```
memberId    : string   sampleichi
fullName    : string   見本 一郎
displayName : string   見本
initial     : string   見
team        : string   team_a
jobTitle    : string   リーダー
role        : string   member
order       : number   30
active      : boolean  true
```

（ドキュメントIDには、この人の会社Googleアカウントのメールアドレスを小文字で入れます）

### `fullName` について（重要）

シフト表・生産性データとの突き合わせキーになります。
**姓だけの部分一致で照合すると事故ります。** 実際のメンバーには、

- 姓の先頭1文字が同じ人が複数いる（前方一致だと取り違える）
- スプレッドシート側に異体字の揺れがある
  （「渡辺／渡邊」「斎藤／齋藤」のような、字は違うが読みが同じ組み合わせ）

というケースがどちらも存在します。
**スプレッドシートに書かれているとおりの文字**を、コピー＆ペーストで入れてください。
手で入力すると、変換候補の先頭に出る字を選んでしまって揺れます。

どちらの字を正とするかは、Phase 0 で確定済みの表記に合わせてください。

### `order` について

メンバー一覧の並び順です。`orderBy('order')` で全件取得するため、
**全員に値が入っていないと、抜けている人が一覧に出てきません。**

管理職 → チームA → チームB の順に、10刻みなどで余裕をもたせておくと
後から間に挿入しやすくなります（例: 10, 20, 30, ...）。

### `role` について

| 値 | 権限 |
|---|---|
| `member` | 自分の投稿の編集・削除のみ |
| `admin` | それに加えて**他人の投稿も削除できる**（Phase 4 で使用） |

`admin` はセンター長・チーフの2名程度に留めてください。
この値は Firestore にしかなく、クライアントからは書き換えられません。

---

## 3. 登録後の確認

### ✅ 許可リストが効いているか

1. 登録したアカウントでログイン → アプリが表示される
2. 登録していないGoogleアカウントでログイン
   → 「利用権限がありません」と表示され、自動でサインアウトされる

### ✅ 未ログインでデータが読めないか

ブラウザのシークレットウィンドウで公開URLを開き、開発者ツールのコンソールで
次を実行します。**すべて `permission-denied` で失敗すれば正常です。**

```js
const { getFirestore, collection, getDocs, doc, getDoc } =
  await import("https://www.gstatic.com/firebasejs/12.17.1/firebase-firestore.js");
const { initializeApp } =
  await import("https://www.gstatic.com/firebasejs/12.17.1/firebase-app.js");
const { FIREBASE_CONFIG } = await import("./firebase-config.js");
const db = getFirestore(initializeApp(FIREBASE_CONFIG, "probe"));

for (const c of ["members","teams","shifts","goals","metrics","syncStatus",
                 "feedPosts","boardPosts","surveys"]) {
  try {
    const s = await getDocs(collection(db, c));
    console.error("❌ 読めてしまった:", c, s.size, "件");
  } catch (e) {
    console.log("✅ 拒否:", c, e.code);
  }
}
```

> `members` は「自分の1件だけ」読める設計ですが、未ログインでは
> `signedIn()` が false になるため、その1件も読めません。

### ✅ 許可リスト外のアカウントで他人の情報が読めないか

許可リストに載っていないGoogleアカウントでログインした状態
（サインアウトされる前）に、上のスクリプトを実行しても
`members` の**一覧取得は拒否**されます。読めるのは自分宛の1件だけで、
その1件は存在しないため、他人のメールアドレスも氏名も権限も漏れません。

### ✅ 登録内容が本当にサーバーに入っているか

コンソールの表示だけを信じないでください（冒頭の④）。
ログインできる状態になったら、アプリのタブの開発者コンソールで次を実行します。

```js
const { getApp } = await import("https://www.gstatic.com/firebasejs/12.17.1/firebase-app.js");
const { getFirestore, collection, getDocs } =
  await import("https://www.gstatic.com/firebasejs/12.17.1/firebase-firestore.js");
const db = getFirestore(getApp());
const bad = [];
const ms = await getDocs(collection(db, "members"));
ms.forEach(d => {
  const v = d.data(), who = v.displayName || d.id;
  if (d.id !== d.id.toLowerCase().trim()) bad.push(who + ": ドキュメントIDに大文字か空白");
  if (typeof v.order !== "number")  bad.push(who + ": order の型が " + typeof v.order);
  if (v.active !== true)            bad.push(who + ": active の型が " + typeof v.active);
  if (!["member","admin"].includes(v.role)) bad.push(who + ": role が " + JSON.stringify(v.role));
});
console.log(ms.size + "名 / " + (bad.length ? "⚠\n" + bad.join("\n") : "✅ 型はすべて正常"));
```

---

## 4. スクリプトから一括投入する

人数が多いとき、またはコンソール入力で型を間違えたときは、こちらが確実です。
**型を保証でき、幽霊ドキュメントも発生しません。**

### 手順

1. **ルールを一時的に書き込み可にする**（Firestore → ルール）

   ```javascript
   match /members/{email} {
     allow get: if signedIn() && email == myEmail();
     allow list: if signedIn();                        // ← 一時
     allow create, update, delete: if signedIn();      // ← 一時
   }
   match /teams/{teamId} {
     allow read: if signedIn();                        // ← 一時
     allow write: if signedIn();                       // ← 一時
   }
   ```

   > この間、Googleアカウントを持つ誰でもメンバー一覧を読み書きできます。
   > 作業は数分で終わらせ、必ず元に戻してください。

2. **投入用のページをローカルに作る**
   リポジトリ直下に一時的な HTML を置き、`setDoc()` で全件書き込みます。
   ページには実名とメールアドレスが並ぶため、**絶対にコミットしないこと。**
   投入が終わったら削除します。

3. **サーバーから読み直して検証**（件数と型）

4. **ルールを元に戻して公開**

   ```javascript
   allow list: if isMember();
   allow create, update, delete: if false;
   allow read: if isMember();
   allow write: if false;
   ```

### ルール編集時の注意

コンソールのエディタで部分的に書き換えると、閉じ括弧を消して
`Line N: missing '}'` のエラーになりがちです。
**リポジトリの `firestore.rules` を全選択して貼り直す**のが確実です。

---

## 5. メンバーの追加・変更

| やりたいこと | 操作 |
|---|---|
| 新しいメンバーを追加 | `members` にドキュメントを1件追加（IDは小文字のメールアドレス） |
| 退職・異動 | ドキュメントは**消さず** `active` を `false` に。過去の投稿の表示名が「?」にならないようにするため |
| メールアドレス変更 | 新しいメールアドレスで新規ドキュメントを作り、`memberId` は**同じ値**にする。旧ドキュメントは削除する |
| 権限を変える | `role` を `member` ⇄ `admin` |

**`memberId` は絶対に変更しないでください。** 投稿・リアクション・既読・投票が
すべてこの値で紐づいているため、変えると過去のデータの持ち主が分からなくなります。
