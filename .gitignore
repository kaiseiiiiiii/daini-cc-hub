# 一時ツール — 実名・メールアドレスを含むため絶対にコミットしない。
# マスタ投入や認証まわりの調査で作る使い捨てページはこの名前で作ること。
seed*.html
debug-*.html

# サービスアカウント鍵（Phase 5）。GAS のスクリプトプロパティに置くため、
# 万一ローカルにダウンロードしてもコミットされないようにしておく。
*serviceaccount*.json
*service-account*.json
*-firebase-adminsdk-*.json

# OS / エディタ
.DS_Store
Thumbs.db
*.swp
